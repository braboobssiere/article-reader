name = "private-article-reader"
main = "src/index.ts"
compatibility_date = "2025-03-01"

# Turnstile is DISABLED by default.
# To enable, uncomment the lines below and set the secrets.
# [vars]
# TURNSTILE_ENABLED = "true"

# If you have a KV namespace for caching, uncomment:
kv_namespaces = [
  { binding = "ARTICLE_CACHE", id = "your-kv-id" } ]
