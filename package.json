{
  "name": "private-article-reader-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "update": "npx npm-check-updates -u && npm install"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "hono": "^4.6.15",
    "linkedom": "^0.18.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250312.0",
    "wrangler": "^3.107.3"
  }
}
